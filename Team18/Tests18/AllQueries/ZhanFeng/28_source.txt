procedure Q {
z = a * b * 3;
if ( ( 0 * a ) < b ) then {
  print answer;}
else {
  z = a * b / c - a / b / ( c - z );
  while ( b == ( b / c ) ) {
      x = a * ( b / c - a / b ) / c - z; }
  if (x == 3) then {
      y = x;
    } else {
      print x;
     read R; }
 z = 3 / ( a * b );
 x = b / c;
 z = 3 + y;}
y = answer + 42;}
