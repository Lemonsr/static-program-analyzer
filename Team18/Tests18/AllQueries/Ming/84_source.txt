procedure one { read me;
    print you;
    if (x != y) then {
        call two;
    } else { call three; }
    zero = 35 % 0;}
procedure two { ten = five * two;
    six = nine - three;
    call three; 
    call four; }
procedure three { spring = fall;
    summer = hot; }
procedure four {    cs3203 = cs3230 - cs27;
    while (windows >= macOS) {
        print linux;    
        call five; 
        call six; }}
procedure five { read thisProcedureDoesNotUseAnything; }
procedure six { print thisProcedureDoesNotModifyAnything; }