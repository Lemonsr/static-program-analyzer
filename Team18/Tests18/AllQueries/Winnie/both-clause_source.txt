procedure Main {
    read x;
    read y;
    y = v % v % x / y;
    if (x < 0) then {
        read w;
        print z;
        y = z - y - v / y / y % v;
    } else {
        read z;
        u = u / x % y - u + x / u;
        print w;
    }

    read u;
    read z;
    while (z < 10) {
        print z;
        read x;
        y = v % u + u - v + v;
    }

    if (z < x + y) then {
        if (x < y + 10) then {
            read v;
            while (x < y + 50 - z) {
                x = w + w % x;
                print x;
            }
            read z;
        } else {
            while (z < x * 2) {
                read y;
                u = y + y / u * z;
                print y;
            }
            z = v - v - x % u % x;
        }
    } else {
        print z;
        read z;
        x = u + y + v;
    }

    print x;
    u = y % u * v - y + x;
    read z;

    while ((x < y) && (z > x)) {
        read x;
        if (z < 10) then {
            y = z + 1;
            print x;
            z = z * z / y;
        } else {
            print z;
            read y;
            y = z / x * 2;
            print z;
        }
        w = v % w + y;
        print x;
    }
    read z;
}